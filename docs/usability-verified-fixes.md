# CamNook verified usability fixes

- **U40: rejected payment reference clears both correction inputs.** Chrome synthetic reference rejection erased the reference and selected proof. Initial payment submission now preserves native fields on failure and resets after success. `check-payment-submission-recovery.mjs` passes: rejected reference remains, change only the reference, reuse the same proof, receive accepted feedback. All sixteen payment tests passed. No transfer or hosted submission occurred.

- **U39: failed payment-proof upload loses the selected image.** Chrome actual payment panel with synthetic upload failure reset the native file picker; retry was blocked with Please select a file. Dispatch the proof action in a transition and reset only after success. Repeatable `check-payment-proof-recovery.mjs` passes: choose once, fail, retry same file, success; screenshot confirms successful reset. Seven relevant payment tests and targeted lint passed. No hosted payment or upload occurred.

- **U38: closing the optional map loses keyboard position.** Production KYC Add map pin → Cancel leaves BODY focused. Restore focus to Add/Adjust map pin after Cancel, Confirm and Remove; expose expanded state and the controlled editor. Enhanced three existing regressions failed before/passed after; nine pin/map tests and targeted lint passed. Chrome actual picker fixture verified all three exits focus the appropriate trigger with expanded=false. Synthetic coordinates only; no profile saved.

- **U37: renters cannot inspect additional listing photos.** Production owner preview counts three photos, but renter page exposes one and no gallery. Added a native expandable gallery using all loaded photos in order, larger uncropped images, descriptions and position labels. Chrome fixture verified all three images loaded, click expansion and Enter collapse; local Next Development listing exposes the actual three-photo gallery. Ordered-photo/empty-gallery regression, targeted lint, TypeScript and diff check passed. No hosted data changed.

- **U36: blocked dates cannot be inspected or undone.** Production has only creation fields; successful synthetic creation cleared them without showing the range. Added a narrow admin-only read API, strict result parsing, current/upcoming manual and maintenance list, separate load-error retry, and removal through the existing audited release operation. Chrome: failed removal retained the range, retry removed it, and a newly created inclusive date range appeared correctly. Two frontend regressions and SQL suite 023 cover recovery, malformed/load results, renter/anonymous denial, booking protection, unchanged public column grants and retained release audit. All 75 migrations applied locally; three database suites and full frontend checkpoint passed. No hosted block changed; migration unreleased.

- **U35: single available time requires an unnecessary extra selection.** Chrome morning-blocked range offered only 17:00 but left Choose a time and no estimate. The return-date selection now preselects a sole valid time using the same availability calculation as the dropdown. Chrome produced the 17:00 estimate/Continue automatically; a later range with two choices removed continuation and requested a choice. Meaningful interaction regression replaced a weak static test, failed before/passed after; fourteen relevant tests, lint, TypeScript and diff check passed.


- **U34: availability continuation discards settings.** Production Chrome: select Saturday, Continue to preview, return to Availability → Saturday unselected. No policy save submitted. Continue now submits the availability form and redirects to preview only after server success. It shares the form’s pending and valid-area disabled state; optional blocked-date form remains separate. Chrome error/correction preserves Saturday and reaches preview. Twenty-five relevant tests pass, covering intent, save failure/no redirect, successful redirect and disabled continuation; lint/TypeScript/diff checks passed. No hosted policy saved.


- **U33: camera setup continuation discards edits.** Production Chrome: enter a changed price, Continue to availability, return to Camera → original price. No save was submitted. The continuation control now submits its associated camera form with an explicit intent; the server action redirects to that camera’s availability only after saving. Pending form submissions are guarded. Five actual-action/form tests pass, including error retention, retry, saved price/kit and fixed destination. Chrome synthetic validation error stays on camera; correcting the kit reaches availability. Lint, TypeScript and diff check passed; no hosted camera saved.


- **U32: temporary verification failure clears the code.** Actual OTP form recovery test showed an empty input after a temporary failure, preventing retry without re-entry. The input now retains its value in component state only. New regression failed before/passed after and verifies identical retry payloads; 32 relevant auth tests, lint and TypeScript passed. Chrome screenshot confirms the dummy value remains after the error. DOM token reads are redacted and were excluded as evidence. No real code or provider request used.

- **U31: request validation hides the actionable error.** Actual RequestForm in Chrome with synthetic name rejection kept “Enter your name” hidden while review displayed a generic error. The action result now reopens details for contact/meetup/purpose/city field errors, using existing focus handling and preserving values. Chrome verified visible error, correcting only the name, reviewing, and explicit retry with complete payload. New recovery regression failed before/passed after; four component tests, lint, TypeScript, and diff check passed. No hosted request submitted.

- **U30: coordinate Enter accidentally submits the profile.** Chrome actual ResidentialMap inside a synthetic parent form: entering coordinates and pressing Enter yielded Profile submissions: 1, Selected address: None. Coordinate fieldset now prevents implicit profile submission and invokes draft placement/validation. Chrome after fix selected the pin with zero submissions. New regression failed before/passed after for both fields and invalid coordinates, while explicit Save still works; six map tests, lint and TypeScript passed. No live profile submitted.

- **U29: availability confirmation conceals unsaved edits.** Chrome synthetic availability form retained “Availability saved” after checking Wednesday. Form now tracks edits since submission, shows Unsaved changes after a later edit, and suppresses stale confirmation while saving. Enhanced recovery regression failed before/passed after and verifies revised weekday payload; Chrome edit/resave, lint, TypeScript, and diff checks passed. No hosted policy saved.

- **U1: restoring a schedule loses estimate and Continue.** Production Chrome: Sep 14–17, 2026, 09:00 yielded ₱2,350; reselecting the identical range/time removed the quote and Continue (both counts zero). Real local component reproduced the failure. One-line fix includes edit generation in the auto-quote request identity. `scripts/usability/check-schedule.mjs` passed in Chrome after failing before the fix: initial quote, stale continuation removed during edits, identical selection requoted, request URL preserved. Uses synthetic quote responses because Development scheduling is disabled. No deployed verification claimed.
- **U2: quote failure has no usable retry.** Chrome harness displayed “please retry” with zero retry buttons. Added Retry estimate. First verification failed because automatic form reset cleared the time dropdown while React's summary retained 09:00. Changed quote submission to `onSubmit` + `startTransition(formAction)` (supported by installed Next server-action guide), preserving browser inputs. `scripts/usability/check-recovery.mjs` now passes: retry exposed, no continuation on error, retry without editing, error cleared, request time preserved. U1 regression rerun passed. No deployed verification claimed.
- **U3: paused listings advertise Check dates.** Development Chrome catalog offered Check dates for both cameras although the Canon detail page showed Scheduling unavailable. Shared `canScheduleRental` predicate now governs catalog and schedule component. Paused cards show “Not accepting rental requests right now” and View details; eligible cameras retain Check dates. Chrome verified both paused cards' status and links. Six eligibility cases plus three existing component tests passed; lint/typecheck passed.
- **U4: free afternoon hidden by unavailable morning.** Real component in Chrome with synthetic Monday 08:00–12:00 unavailability and 09:00/17:00 approved times disabled all of Monday. `calendarDateStatus` now evaluates all approved times and, for return dates, checks a common time with an unobstructed rental range. Chrome verified Monday selectable → Thursday return → only 17:00 offered → Continue with 17:00. Four new calendar cases cover later free slots, today's elapsed morning, all-day unavailability, and intervening bookings. Twenty relevant tests, lint, and typecheck passed. Hosted scheduling verification remains blocked by Development policy.
- **U5: Change dates erases the chosen schedule.** Production Chrome request page → Change reopened a completely blank calendar. All request-page camera-return links now carry only pickup/return/time; camera page validates them against current policy/availability and initializes the calendar/month, then obtains a fresh quote. Seven unit cases cover round-trip and invalid/stale inputs. Chrome harness review → Change dates restored both selected endpoints and 09:00 and produced Quote ready / Continue. No Production changes or personal profile submissions.
- **U6: past unreviewed pickup says No action needed.** Production request detail with pickup/return in the past still promised notification and told the renter to do nothing. Added explicit past-pickup guidance and Choose new dates link; retained persisted review state and existing confirmed/active behavior. Account list uses the same guidance. Extracted the existing booking action card into a reusable component so Chrome harness verifies the exact UI with synthetic history. Six status tests passed, including boundary/future/malformed dates and active rentals. Chrome card displays passed pickup, still awaiting review, and a working catalog destination; no booking was cancelled or expired.
- **U7 (isolated follow-up): calendar overflows a 320px phone.** After constraining an unrelated harness select, page width was 321px and the date grid/time picker extended beyond the 304px content edge. Allowed form/fieldset shrinkage and reduced narrow-screen month-heading size/gaps. Chrome now measures 320px page / 320px viewport; September → October navigation, Oct 1–5 selection across no-handoff weekend, and actionable quote all pass. Lint passed. This follow-up is separate from external release commit `9eb35cd`.

- **U8 (isolated follow-up): failed address lookup requires page reload.** Chrome real selector with a synthetic temporary 503 retained Region VII but showed no province choices or retry control. Added Retry area lookup for initial and child failures. Chrome retry restored Cebu province choices with the region and unsaved address note preserved; error and retry disappeared. Two meaningful interaction tests failed before/passed after, covering initial/child retries and avoiding surrounding form submission. Six location tests and lint passed. No hosted profile was submitted.

- **U9 (isolated follow-up): reviewed request omits renter details and can submit before confirmation.** Chrome real redesigned form entered review while invoking the synthetic action; payload contained schedule/operation only, missing name, phone, meetup area, purpose, and shooting city. The actual server rejects these missing values. Both steps now stay mounted with `hidden` visibility; Enter from details is guarded to show review. Chrome Continue now shows Not submitted; explicit Request rental sends all reviewed fields. Interaction regression covers keyboard review, complete payload, edit/retry, and stable operation ID; failed before/passed after. Full suite 709 passed / 2 skipped, lint/typecheck passed. No hosted booking created.

- **U10 (isolated follow-up): stale address search results remain selectable.** Chrome real ResidentialMap with synthetic responses showed a Cebu result after a different query failed. Clear suggestions/status when editing, clear suggestions before searching, and ignore superseded responses. Chrome confirms successful lookup still offers its result and failed changed query leaves no old result. Two regression tests failed before/passed after (changed-query failure and late response); five map tests, lint and typecheck pass after correcting a test-only locator option. No hosted profile saved.

- **U11 (isolated follow-up): failed sign-in clears email.** Synthetic failure exercises the real LoginForm without sending mail. Regression test captured an empty email after failure before the fix. Controlled email now persists through form reset; Chrome screenshot confirms email remains with the error. Retry test verifies same email and return destination reach the action twice; four auth tests, lint/typecheck passed. Browser text snapshots omit email values, so visual verification was used.

- **U12 (isolated follow-up): owner stage cards lead to missing or generic destinations.** Production zero Payment review link has no matching region. Real redesigned overview locally sent all nine stages to the same bookings page. Empty cards now remain readable without dead links; populated overview links include their queue fragment. Chrome fixture opens `/admin/bookings#queue-review` with the review region; regression failed before/passed after. Four dashboard tests, lint and typecheck passed after correcting a synthetic fixture type mismatch. No owner record changed.

- **U13 (isolated follow-up): past pickup is presented as a retryable quote problem to the owner.** Production review for a past pickup showed quote unavailable / Retry readiness check. Installed database migration rejects pickup at or before its current timestamp. Readiness now adds pickup_passed (including with an old quote) instead of the misleading quote reason. Extracted the existing readiness panel for direct Chrome verification; screenshot shows passed-pickup guidance and no quote retry. Three new cases failed before/passed after; 31 relevant tests passed. Full suite 716 passed / 2 skipped, lint, TypeScript, and production build passed. No booking state changed.

- **U14 (isolated follow-up): camera edits silently reduce accessory quantities.** Production renter kit shows two batteries; camera editor lists Battery once with “Each item is included once.” Existing action hardcodes quantity one and database save replaces active accessory rows. Added explicit quantity formatting and parsing (for example 2 × Battery), positive integer/unique-name validation, and preserved commas inside item names. Chrome synthetic price-only save retains two batteries. Action-level interaction test exercises actual save through a mocked authenticated RPC; eight parser cases cover round-trip, legacy single items, invalid quantities, and duplicates. Nine tests, lint/typecheck passed. No hosted camera changed.

- **U15 (isolated follow-up): camera validation failure discards edits.** Chrome synthetic invalid quantity reset an edited description and kit to saved defaults. CameraDetailsForm now preserves inputs while dispatching its server action via startTransition. Chrome confirms invalid input/error remains editable, description survives, and changing only the quantity succeeds. Actual action interaction regression failed before/passed after and verifies preserved description at the mocked RPC boundary. Ten relevant tests, lint/typecheck passed.

- **U16 (isolated follow-up): availability validation silently reverts settings.** Chrome real policy form with synthetic invalid-time response cleared the newly selected Tuesday and restored 09:00 despite showing a validation error. Preserved native inputs while dispatching the server action in a transition. Chrome now retains Tuesday, changed enabled state, and invalid time; correcting only time succeeds. Regression checks the corrected payload carries both weekdays, disabled availability, and 17:00; failed before/passed after. Four relevant tests, lint/typecheck passed. No hosted handoff policy changed.

- **U17 (isolated follow-up): invalid blocked range clears both dates.** Chrome actual form with synthetic date-order rejection erased the range. Transition dispatch retains inputs on failure; explicit form reset keeps successful submission behavior. Chrome error kept Sep 20/19, correcting only Through to Sep 21 succeeded and cleared both fields. Actual server-action integration regression verifies retained values, inclusive-end conversion at mocked RPC, and success reset. Three form tests and lint passed. No hosted dates blocked.

- **U18 (isolated follow-up): temporary booking-list failure has no retry.** Production Chrome showed only a load error; browser reload restored the queue. Added native GET Retry bookings to the actual error component, ensuring a fresh page request. Chrome fixture clicked Retry, reached `/admin/bookings`, displayed the review queue, and removed the error. Lint passed. This small navigation-only change uses the browser check rather than a redundant markup test. Hosted recovery was observed before the code change; new control is not deployed.

- **U19 (isolated follow-up): booking-detail back link targets dashboard.** Production Chrome clicked Back to review queue and reached `/admin`; isolated page labels that target Back to bookings. Corrected to `/admin/bookings`; extracted the exact link component for Chrome harness verification. Clicking it reaches the loaded booking-review queue. Lint passed; no deployed verification claimed.

- **U20 (isolated follow-up): request steps lose focus and context.** Chrome Continue left focus on BODY and review heading at -30px above the viewport. On step changes, focus the new heading without affecting initial page focus. Chrome now reports review/details heading focused at top 0; Tab after Edit reaches Name. Enhanced existing submission regression failed before/passed after, also retaining explicit confirmation and complete retry payload. Lint passed.

- **U21 (isolated follow-up): cancellation failure clears the explanation.** Real renter component in Chrome with synthetic failure reset the textarea. Controlled reason now survives failure and retry. Chrome verified both; interaction regression failed before/passed after and confirms unchanged reason, booking ID, and operation ID in both submissions. Five resolution tests passed. No hosted cancellation submitted.

- **U22 (isolated follow-up): unconfirmed request lacks a direct booking-check action.** Chrome failure told the renter to check bookings but exposed no link in the error. Linked that instruction to `/account`; Chrome verified the link in the actual error and exact destination. Existing account route/retry was verified separately in Production. Enhanced existing submission regression and lint passed. No request created.

- **U23 (isolated follow-up): renter prices silently round away centavos.** Owner inputs permit hundredths, but catalog/detail/quote formatters forced zero decimal places. Chrome synthetic quote for ₱450.50 + ₱1,000.25 displayed ₱451/₱1,000/₱1,451. Shared PHP formatter now retains two decimals; Chrome shows ₱450.50/₱1,000.25/₱1,450.75 and local catalog preserves prices with two decimals. Currency regression covers subtotal, deposit, total, zero; four relevant tests and lint passed. No hosted price changed.

- **U24 (isolated follow-up): photo upload failure discards file selection.** Chrome actual photo form with synthetic upload failure showed No file chosen after error. Preserve file on failure, reset only on success. `check-photo-recovery.mjs` passes through the native Chrome chooser: choose generated image once, fail, retry successfully, error clears. Screenshot confirms successful reset to No file chosen. DOM-only test attempt did not reproduce native file/required behavior, so the repeatable browser regression is the harness. Existing three owner-form tests and lint passed. No hosted upload occurred.

- **U25 (isolated follow-up): camera load failure forces a catalog detour.** Production Chrome showed Camera details unavailable and told the renter to return to catalog. Added a direct native GET retry. Preserve only valid pickup/return/time fields, excluding unrelated query values. Chrome retry restored Sep 14–15 and 09:00, cleared error, and produced a fresh estimate/Continue. Query-boundary regression, lint, and TypeScript passed. No hosted state changed.

- **U26 (isolated follow-up): unpublish failure is silent.** Owner inventory exposes Unpublish; action ignored RPC errors. Chrome original form with synthetic failure showed zero alerts/statuses. Extracted existing control into a stateful form, added pending/error/success feedback, and return explicit outcomes from the action. Chrome retry shows Camera unpublished and disables repeat submission. Actual-action regression verifies failure then success for the same camera; failed before/passed after. Four form tests passed. No hosted listing unpublished.

- **U27 (isolated follow-up): rejected report dates revert to defaults.** Chrome real report panel and resolver submitted Sep 20/19 but redisplayed Sep 1/13 with the error. Invalid selections now keep valid endpoints; malformed/missing endpoints stay blank. Chrome corrects only end to Sep 21, preserves start, clears error, and displays the synthetic report. Two regressions failed before/passed after; nine portfolio tests and lint/TypeScript passed. Invalid status still prevents a database report query.

- **U28 (isolated follow-up): saved camera confirmation survives unsaved edits.** Chrome saved the camera, changed price to 600, and still showed Camera details saved. Track edits since submission; show neutral Unsaved changes after editing and suppress stale success while pending. Chrome resave confirms new price and preserved kit quantities. Enhanced actual-action regression failed before/passed after; four form tests and lint passed.
